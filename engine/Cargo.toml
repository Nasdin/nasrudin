[workspace]
members = [
    "crates/core",
    "crates/rocks",
    "crates/pg",
    "crates/lean-bridge",
    "crates/derive",
    "crates/ga",
    "crates/api",
    "crates/mcp",
    "crates/importer",
    "crates/embed",
    "crates/llm",
]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "MIT"

[workspace.dependencies]
# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = { version = "1.0", features = ["unbounded_depth"] }
schemars = { version = "0.8", features = ["preserve_order"] }

# Async runtime
tokio = { version = "1.49", features = ["full"] }
tokio-stream = { version = "0.1", features = ["time", "sync"] }

# Error handling
anyhow = "1.0"
thiserror = "2.0"

# Observability
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

# Resource detection (for worker auto-sizing)
num_cpus = "1.16"
sysinfo = "0.32"

# Hashing & encoding
blake3 = "1.5"
xxhash-rust = { version = "0.8", features = ["xxh64"] }
hex = "0.4"
data-encoding = "2"
bloomfilter = "1"
base64 = "0.22"

# Web framework
axum = { version = "0.8", features = ["ws"] }
axum-extra = { version = "0.10", features = ["typed-header"] }
axum-login = "0.18"
tower-sessions = { version = "0.14", features = ["memory-store"] }
tower = "0.5"
tower-http = { version = "0.6", features = ["cors", "trace", "compression-gzip", "compression-br"] }
tower_governor = "0.8"
governor = "0.10"

# Authentication
password-auth = "1"

# Database - RocksDB (embedded theorem store)
rocksdb = { version = "0.24", features = ["multi-threaded-cf"] }

# Database - SeaORM 2 (PostgreSQL ORM for users/auth/workers)
sea-orm = { version = "2.0.0-rc.28", features = [
    "sqlx-postgres",
    "runtime-tokio-rustls",
    "macros",
    "with-uuid",
    "with-chrono",
    "with-json",
] }
sea-orm-migration = { version = "2.0.0-rc.28", features = [
    "sqlx-postgres",
    "runtime-tokio-rustls",
] }

# Common types
uuid = { version = "1.20", features = ["v4", "v5", "serde"] }
chrono = { version = "0.4", features = ["serde"] }
rand = "0.9"

# Env
dotenvy = "0.15"

# Protobuf
prost = "0.14"
bytes = "1.11"

# Embeddings (Phase B)
fastembed = { version = "4", default-features = false, features = ["hf-hub-rustls-tls", "ort-download-binaries"] }
instant-distance = { version = "0.6", features = ["with-serde"] }
memmap2 = "0.9"

# LLM router (Phase C)
aes-gcm = "0.10"
rand_core = { version = "0.6", features = ["std"] }
async-trait = "0.1"
futures = "0.3"

# Cluster steerer + paid Researcher (Phase D)
arc-swap = "1"
dashmap = "6"

# Cold-tier RocksDB encoding for the Mathlib corpus (~195k entries).
# bincode is compact + zero-copy via existing serde derives; lru caps the
# in-process working-set on top of the RocksDB block cache.
bincode = "1.3"
lru = "0.12"
