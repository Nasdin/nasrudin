[package]
name = "nasrudin-core"
version.workspace = true
edition.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
xxhash-rust = { workspace = true }
hex = { workspace = true }
thiserror = { workspace = true }
blake3 = { workspace = true }
# Zero-copy binary serialization for the cold-tier corpus. The hot
# `Axiom`/`Expr` types live here, so rkyv's derives must too.
rkyv = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
