//! Stripe billing endpoints — placeholder.
//!
//! Declared via `pub mod billing` in handlers/mod.rs as part of the
//! Phase 1+2 billing migration commits (91586a4 / 4c7088f). Real
//! handlers (subscribe, webhook, customer-portal, etc.) land in a
//! follow-up commit; this empty module exists only so the workspace
//! compiles cleanly during the rollout window.
